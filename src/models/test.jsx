import React, { useRef } from 'react'
import birdScene from '../assets/3d/bird.glb';
import { useAnimations, useGLTF} from '@react-three/drei';
import { useFrame , useEffect} from '@react-three/fiber';

const test = () => {
  return (
    <mesh eifshiwefwiufujnsgi ref = {useRef}>test</mesh>
  )
}

export default test